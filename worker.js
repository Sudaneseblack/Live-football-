export default {async fetch(request, env){const u=new URL(request.url);if(!u.pathname.startsWith('/api/'))return fetch(request);if(!env.API_FOOTBALL_KEY)return Response.json({error:'API key not configured'},{status:503});let endpoint=u.pathname.replace('/api/','');let params=new URLSearchParams(u.search);if(endpoint==='today'){params.set('date',new Date().toISOString().slice(0,10));endpoint='fixtures'}if(endpoint==='live'){params.set('live','all');endpoint='fixtures'}const target='https://v3.football.api-sports.io/'+endpoint+'?'+params.toString();const r=await fetch(target,{headers:{'x-apisports-key':env.API_FOOTBALL_KEY}});return new Response(await r.text(),{status:r.status,headers:{'content-type':'application/json','cache-control':'public,max-age=15'}})}}